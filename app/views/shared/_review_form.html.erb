<!-- Need to CONFIRM: line 4 -->
<!-- This is the best way to connect the routes considering -->
<!-- we want to have the review on the Gear show page -->
<%= simple_form_for [@booking, @review] do |f| %> 
  <%= f.input :gear_content %>
  <%= f.input :user_content %>
  <%= f.association :bookings %>
  <%= f.hidden :booking_id, value: @booking.id %>
  <%= f.button :submit %>
<% end %>
